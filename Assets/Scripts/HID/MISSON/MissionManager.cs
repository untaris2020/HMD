using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public class MissionManager : MonoBehaviour
{
    
    // public static DebugManager Instance;
    // Start is called before the first frame update
    void Start()
    {
       // string InputJSON = "onsiteMissionJSON.json";
       // MissionContainerInstance = MissionContainer.CreateFromJSON(InputJSON);

       // Debug.Log("Spacewalk ID: " + MissionContainerInstance.SuperMissions[1]);
        //Stream stream = new FileStream("onsiteMissionJSON.json", )
    }

    // Update is called once per frame
    void Update()
    {

    }

    
}
